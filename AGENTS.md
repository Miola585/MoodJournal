Project: Mood Journal — This is an attempt to build a jourling app/website that allows uesers to keep track of their emotions and build healthy habits to regulate them.
Target user: Young adults, Teenagers, Anyone who wants to be able to journal and express themselves without judgment
My skill level: intermediate level
Goal: Collegestudent working on multiple projects with little experience in Machine Learning but with expreicne in python, java, data structures and algorithm.
Stack: Vite + React + CSS, with browser localStorage for current data storage
Visual style: Keep the warm peach, cream, and brown palette unless the user asks for a new direction. Dark mode should follow the same warm contrast system, with readable bright text on dark backgrounds.

Commands
- Install: `npm install`
- Dev: `npm run dev`
- Build: `npm run build`
- Test: `npm test`
- Lint: `npm run lint`

Do
- Read existing code before modifying anything
- Match existing patterns, naming, and style
- Handle errors gracefully — no silent failures
- Keep changes small and scoped to what was asked
- Run dev/build after changes to verify nothing broke
- Ask clarifying questions before guessing
- Check for free dpepndencies before suggesting paod ones, i.e database useage and such
- When changing display settings, make sure font size changes affect headings, buttons, labels, forms, and body text.
- Keep activity cards simple: one completion checkbox for the main activity, not separate checkboxes for every small step.

Don't
- Install new dependencies without asking
- Delete or overwrite files without confirming
- Hardcode secrets, API keys, or credentials
- Rewrite working code unless explicitly asked
- Push, deploy, or force-push without permission
- Make changes outside the scope of the request

When Stuck
- If a task is large, break it into steps and confirm the plan first
- If you can't fix an error in 2 attempts, stop and explain the issue

Testing
- Run existing tests after any change
- Add at least one test for new features
- Never skip or delete tests to make things pass

Git
- Small, focused commits with descriptive messages
- Never force push

Response Style
- always respond with clear & concise messages
- use plain English when explaining to the User
- avoid long sentences, complex words, or long paragraphs
